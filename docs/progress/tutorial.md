# Git Commands for Publishing Documentation

Run the following commands from the root of the repository.

```bash
git status
git add mkdocs.yml docs .github .gitignore
git commit -m "Add MkDocs documentation site and weekly progress"
git push origin main




